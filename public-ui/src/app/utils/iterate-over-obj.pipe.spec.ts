import { IterateOverObjPipe } from './iterate-over-obj.pipe';

describe('IterateOverObjPipe', () => {
  it('create an instance', () => {
    const pipe = new IterateOverObjPipe();
    expect(pipe).toBeTruthy();
  });
});
